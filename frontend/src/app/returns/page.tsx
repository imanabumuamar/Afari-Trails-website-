import { PageShell } from "@/components/ui/PageShell";

export default function ReturnsPage() {
  return (
    <PageShell label="Support" title="Returns" description="Returns policy — coming soon." />
  );
}
